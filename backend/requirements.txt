# ---- 已存在于 yy 环境(按实际版本)----
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
httpx==0.28.1

# ---- 本计划新增 ----
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0
pytest==8.3.4
pytest-asyncio==0.25.0

# ---- D2 新增 ----
passlib==1.7.4
# passlib 与 bcrypt>=4.1 不兼容,固定 4.0.1(FastAPI 官方推荐组合)
bcrypt==4.0.1
email-validator==2.2.0

# ---- D3 新增(文档处理 + RAG 底座)----
# 与 docker-compose 的 qdrant:v1.13.0 服务端匹配(客户端过低会告警不兼容)
qdrant-client==1.13.3
langchain-core==1.5.2
langchain-text-splitters==1.1.2
langchain-openai==1.4.1
openai==2.48.0
pypdf==6.16.1
python-docx==1.2.0

# ---- D5 新增(多智能体编排 + SSE)----
# 与 yy 环境实际版本对齐;create_react_agent 为 langgraph-prebuilt 稳定 API
langgraph==1.2.10
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langsmith==0.10.11
duckduckgo-search==8.1.1

# ---- D8 修正:Docker 构建时发现 yy 环境有但 requirements 缺失 ----
# JWT 编解码(FastAPI 官方推荐组合 python-jose + passlib)
python-jose==3.5.0
# 文档上传 UploadFile(File(...)) 依赖
python-multipart==0.0.32
